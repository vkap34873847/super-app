services:
  - type: web
    name: trading-bot
    runtime: python
    buildCommand: pip install -r trading_bot/requirements.txt
    startCommand: cd trading_bot && gunicorn app:app
    healthCheckPath: /
